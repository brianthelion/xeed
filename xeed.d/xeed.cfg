[xeed.vars]
project: xeed
cachedir: .xeedcache
hashfile: lasthash

[xeed.types.tool.subtypes.clitool]
code:
    : class CliTool(Tool):
    :     def run(self, resolver, **_):
    :         import subprocess
    :         import sys
    :         cmdstr = resolver(self._config_blob.get("cmdstr"))
    :         LOG.debug(f"{cmdstr}")
    :         return subprocess.call(cmdstr,
    :                             shell=True,
    :                             stdin=sys.stdin,
    :                             stdout=sys.stdout,
    :                             stderr=sys.stderr,
    :                             )

[xeed.types.tool.subtypes.pytool]
code:
    : class PyTool(Tool):
    :     def run(self, resolver, **kwargs):
    :         code = resolver(self._config_blob.get("code"))
    :         LOG.debug(f"{code}")
    :         eval(code, kwargs)
    :         return 0